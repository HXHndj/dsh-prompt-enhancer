module.exports = "let dshEnhIdSeq = 0;\r\n// 2026-08-16（方案「设置界面样式与交互对齐官方」）：稳定 id 生成器——\r\n// 组件内以 useState(() => ...)/useRef 固定一次，避免重渲染漂移；\r\n// 用于 label htmlFor / aria-controls / aria-describedby 关联\r\nfunction dshEnhId(prefix) {\r\n  dshEnhIdSeq += 1;\r\n  return (prefix || 'dsh-enh') + '-' + dshEnhIdSeq;\r\n}\r\n\r\nfunction errorKey(code) {\r\n  const map = {\r\n    GUARD: 'errGUARD',\r\n    NO_LLM: 'errNO_LLM',\r\n    NO_MODEL: 'errNO_MODEL',\r\n    UNKNOWN_MODEL: 'errUNKNOWN_MODEL',\r\n    NO_ADAPTER: 'errNO_ADAPTER',\r\n    INVALID_CREDENTIAL: 'errINVALID_CREDENTIAL',\r\n    QUOTA: 'errQUOTA',\r\n    CONTEXT_WINDOW_EXCEEDED: 'errCONTEXT_WINDOW_EXCEEDED',\r\n    EMPTY_RESPONSE: 'errEMPTY_RESPONSE',\r\n    OUTPUT_TOO_LONG: 'errOUTPUT_TOO_LONG',\r\n    TIMEOUT: 'errTIMEOUT',\r\n    ABORTED: 'errABORTED',\r\n    LLM_FAILED: 'errLLM_FAILED',\r\n    NETWORK: 'errNETWORK',\r\n    ALL_MODELS_UNAVAILABLE: 'errAllModels',\r\n    STREAM_THROW: 'errModelUnreachable',\r\n  };\r\n  return map[code] || 'errUNKNOWN';\r\n}\r\n\r\n\r\n// v3.6（单选）：行内测试失败分类——client 端按 models/test 返回的 r.code 映射本地化文案\r\n// （errorKey 表为增强路径 err* 键；本表为设置页「测试」路径 cfgTestFail* 键）。\r\n// 码源实测：TIMEOUT=host 30s 包装（host app.js）；NO_LLM/BAD_ARGS=handler 直出；ABORTED=pingStream\r\n// 中途流中断/取消（host pure.js）；其余=finish.failure.code 透传（UNKNOWN_MODEL/INVALID_CREDENTIAL/\r\n// QUOTA/NO_ADAPTER/EMPTY_RESPONSE）；同步抛错无码=e.code||LLM_FAILED → Generic 兜底。\r\n// 无 STREAM_THROW 项：该码仅产自增强生成路径（host enhance-handlers.js），models/test 永不返回。\r\nfunction testFailKey(code) {\r\n  const map = {\r\n    TIMEOUT: 'cfgTestFailTimeout',\r\n    UNKNOWN_MODEL: 'cfgTestFailUnknownModel',\r\n    INVALID_CREDENTIAL: 'cfgTestFailCredential',\r\n    QUOTA: 'cfgTestFailQuota',\r\n    NO_ADAPTER: 'cfgTestFailNoAdapter',\r\n    NO_LLM: 'cfgTestFailNoAdapter',\r\n    EMPTY_RESPONSE: 'cfgTestFailEmptyResponse',\r\n    ABORTED: 'cfgTestFailAborted',\r\n    NETWORK: 'cfgTestFailNetwork',\r\n  };\r\n  return map[code] || 'cfgTestFailGeneric';\r\n}\r\nfunction makeT(props) {\r\n  if (typeof props.t === 'function') return props.t;\r\n  return (key) => ZH[key] || key;\r\n}\r\n\r\nconst sessionStores = new Map();\r\n\r\nfunction storeFor(sessionId) {\r\n  let s = sessionStores.get(sessionId);\r\n  if (!s) {\r\n    s = { phase: 'idle', backup: '', enhanced: '', error: null, seq: 0, listeners: new Set(), memoryRounds: [], optimized: false, lastModel: '', fallbackUsed: false, clarify: [], clarifyAnswers: [] };\r\n    // v3.3.x（会话切换增强保持）：整页刷新后从 localStorage 恢复未消费的结果态\r\n    try {\r\n      const raw = localStorage.getItem(resultKey(sessionId));\r\n      if (raw) {\r\n        const rr = JSON.parse(raw);\r\n        if (rr && typeof rr.b === 'string' && typeof rr.e === 'string' && rr.e !== '') {\r\n          s.phase = 'result';\r\n          s.backup = rr.b;\r\n          s.enhanced = rr.e;\r\n          s.optimized = true;\r\n        }\r\n      }\r\n    } catch (e) { /* 忽略 */ }\r\n    sessionStores.set(sessionId, s);\r\n  }\r\n  return s;\r\n}\r\n\r\n// v2.1（§2.4）：已优化标记（localStorage 按会话布尔，区分首次与 reload；无内容、不涉隐私）\r\nfunction seenKey(sessionId) {\r\n  return SEEN_KEY_PREFIX + sessionId;\r\n}\r\nfunction writeSeen(sessionId) {\r\n  try { localStorage.setItem(seenKey(sessionId), '1'); } catch (e) { /* 忽略 */ }\r\n}\r\n// v3.0s（发送清空记忆链·用户修正）：发送成功 = 新一轮迭代开始——重置 seen 标记\r\nfunction clearSeen(sessionId) {\r\n  try { localStorage.removeItem(seenKey(sessionId)); } catch (e) { /* 忽略 */ }\r\n}\r\n\r\nfunction subscribe(sessionId, fn) {\r\n  const s = storeFor(sessionId);\r\n  s.listeners.add(fn);\r\n  return () => { s.listeners.delete(fn); };\r\n}\r\n\r\nfunction notify(sessionId) {\r\n  const s = storeFor(sessionId);\r\n  for (const fn of [...s.listeners]) fn();\r\n}\r\n\r\n// v21（P1-3）：组件卸载且状态空闲时释放 store 条目，防止 sessionStores 无限增长（内存泄漏修复）\r\n// v3.3.x（会话切换增强保持）：全局活动会话跟踪 + 结果持久化（localStorage 跨刷新）\r\nlet activeSessionId = null;\r\n\r\nfunction setActiveSession(id) { activeSessionId = id || null; }\r\n\r\nfunction getActiveSession() { return activeSessionId; }\r\n\r\nfunction resultKey(sessionId) { return 'dsh-enh-result:' + sessionId; }\r\n\r\nfunction saveResultStore(sessionId, backup, enhanced) {\r\n\r\n  try { localStorage.setItem(resultKey(sessionId), JSON.stringify({ b: backup, e: enhanced })); } catch (e) { /* 忽略 */ }\r\n\r\n}\r\n\r\nfunction clearResultStore(sessionId) {\r\n\r\n  try { localStorage.removeItem(resultKey(sessionId)); } catch (e) { /* 忽略 */ }\r\n\r\n}\r\n\r\nfunction releaseStoreIfIdle(sessionId) {\r\n  const s = sessionStores.get(sessionId);\r\n  if (!s) return;\r\n  if (s.phase === 'idle' && s.listeners.size === 0 && s.error === null) {\r\n    sessionStores.delete(sessionId);\r\n  }\r\n}\r\n\r\nfunction safeSetDraft(inputActions, text) {\r\n  if (!inputActions || typeof inputActions.setDraft !== 'function') return;\r\n  try { inputActions.setDraft(text); } catch (e) { /* 忽略 */ }\r\n}\r\n\r\nfunction cancelEnhance(sessionId, inputActions) {\r\n  const s = storeFor(sessionId);\r\n  if (s.phase !== 'enhancing') return;\r\n  const seq = s.seq;\r\n  s.seq += 1;\r\n  clearResultStore(sessionId); // v3.3.x\r\n  safeSetDraft(inputActions, s.backup);\r\n  s.phase = 'idle';\r\n  s.enhanced = '';\r\n  s.error = null;\r\n  clearResultStore(sessionId); // v3.3.x-fix：取消即失效持久化结果\r\n  notify(sessionId);\r\n  host.call('cancel', { sessionId, seq }).catch(() => {});\r\n}\r\n\r\n// v4.0.0（专家档澄清卡·取消）：澄清轮无在途请求（澄清信号是已完成响应），仅本地回退——\r\n// 恢复 backup（放弃本次）、清题目与暂存问答、不写记忆、不置 optimized；不发 cancel RPC。\r\nfunction clarifyCancel(sessionId, inputActions) {\r\n  const s = storeFor(sessionId);\r\n  if (s.phase !== 'clarify') return;\r\n  s.phase = 'idle';\r\n  s.clarify = [];\r\n  s.clarifyAnswers = [];\r\n  s.enhanced = '';\r\n  s.error = null;\r\n  safeSetDraft(inputActions, s.backup);\r\n  clearResultStore(sessionId);\r\n  notify(sessionId);\r\n}\r\n\r\n// v16：斜杠命令拆分——保留「/命令 前缀」，仅优化其后正文（§3.6）\r\nfunction splitCommand(draft) {\r\n  const m = /^(\\/\\S+)(\\s+)([\\s\\S]*)$/.exec(draft);\r\n  if (m) return { prefix: m[1] + m[2], body: m[3] };\r\n  return { prefix: '', body: draft };\r\n}\r\n\r\n// v2.2（§6.3/§6.5）：实际模式判定——纯模式选择 + 记忆链携带。\r\n// v2.6.1（记忆链）：返回 { mode, seed, memory }；memory = { rounds: s.memoryRounds }（供 host\r\n// 以真多轮消息注入）。\r\n// 2026-08-18（用户指令）：取消「记忆开 + 首次 → 轻量兜底」——记忆只负责携带 rounds 链，\r\n// 优化模式始终按用户选择执行（v4.0.0 三档 lite/standard/expert 均不兜底）。\r\nfunction resolveActualMode(sessionId, cfg) {\r\n  const s = storeFor(sessionId);\r\n  let mode = cfg.mode;\r\n  let seed = false;\r\n  let memory = null;\r\n  if (cfg.memory) {\r\n    if (s.memoryRounds && s.memoryRounds.length > 0) {\r\n      // 记忆开 + 有记忆链 → 当前模式 + rounds 链（host 真多轮注入）\r\n      memory = { rounds: s.memoryRounds };\r\n    }\r\n    // 记忆开 + 无记忆链 → 当前模式原样执行（无 rounds 注入，不兜底轻量）\r\n  }\r\n  return { mode, seed, memory };\r\n}\r\n\r\n// v4.0.0（评审修复·提示收敛）：prettifyModel（模型 id → 显示名）随 resultFallback 的 {model}\r\n// 分支删除一并移除——其唯一消费者已不存在（fallback 信息仍记入 store 供诊断）。\r\n// v4.0.0（专家档澄清卡）：enhance 增加第 5 参 clarifyOpts——澄清续跑入口（ClarifyPanel 三按钮）：\r\n// 提交 → { answers: [{q,a}] }（host 拼进证据正文 + 写记忆链轮次）；跳过 → { skip: true }\r\n//（歧义保持原文，不产生记忆条目）。首次调用不传该参。\r\nfunction enhance(sessionId, draft, inputActions, draftRef, clarifyOpts) {\r\n  const s = storeFor(sessionId);\r\n  if (s.phase === 'enhancing') {\r\n    const old = s.seq;\r\n    s.seq += 1;\r\n    host.call('cancel', { sessionId, seq: old }).catch(() => {});\r\n  }\r\n  s.backup = draft;\r\n  s.seq += 1;\r\n  const seq = s.seq;\r\n  s.phase = 'enhancing';\r\n  clearResultStore(sessionId); // v3.3.x 新一轮覆盖旧结果\r\n  s.enhanced = '';\r\n  s.error = null;\r\n  // v4.0.0（澄清卡）：新一轮开跑即清旧题目；提交续跑时暂存已答问答（成功应用后并入 memoryRounds，\r\n  // 继续优化不重复问）；跳过不产生**本轮**记忆条目（与 host「skip 只进证据正文」同口径）。\r\n  // v4.0.0（评审修复·中）：连续多轮澄清时**合并**而非覆盖——前轮已暂存问答保留、本轮新答追加\r\n  // 在后，终稿应用后全部入链并入后续证据正文（计划 §一.4/§三.2e）；skip 续跑不携带新答但\r\n  // 不清已暂存（跳过的是本轮歧义，不否决前轮已答）。\r\n  s.clarify = [];\r\n  const roundAnswers = clarifyOpts && Array.isArray(clarifyOpts.answers)\r\n    ? clarifyOpts.answers\r\n        .filter((x) => x && typeof x.q === 'string' && typeof x.a === 'string' && x.a.trim() !== '')\r\n        .slice(0, 3)\r\n    : [];\r\n  // v4.0.0（评审修复·跟进）：合计口径封顶 9（3 题/轮 × ≤3 轮澄清），超限**保留最近**（slice(-9)，\r\n  // 时序不乱）——与 host enhance-handlers 的 answers 上限同口径对齐：host 不再截前 3 条，\r\n  // 双侧合计上限一致 ⇒ 后轮新答案不会被截出证据正文（§三.2c）。\r\n  s.clarifyAnswers = (Array.isArray(s.clarifyAnswers) ? s.clarifyAnswers : []).concat(roundAnswers).slice(-9);\r\n  notify(sessionId);\r\n\r\n  const parts = splitCommand(draft);\r\n  const config = configState.value;\r\n  // v2.2（§6.3）：实际模式判定（纯模式 + 记忆开关/首次兜底）——实际 mode、seed、记忆链随请求传给 host\r\n  const actual = resolveActualMode(sessionId, config);\r\n  const req = { sessionId, seq, text: parts.body, config, mode: actual.mode };\r\n  if (actual.seed) req.seed = true;\r\n  if (actual.memory) req.memory = actual.memory;\r\n  if (s.optimized && actual.memory && actual.memory.rounds.length > 0) req.continue = true;\r\n  // v4.0.0（澄清卡续跑）：answers/skip 透传 host（rpc-schema 只校验 sessionId/text，附加字段直通）；\r\n  // v4.0.0：answers 携带**全部已暂存问答**（含前轮，答案并入证据正文后生成终稿）\r\n  if (clarifyOpts && Array.isArray(clarifyOpts.answers) && s.clarifyAnswers.length > 0) req.answers = s.clarifyAnswers;\r\n  else if (clarifyOpts && clarifyOpts.skip === true) req.skip = true;\r\n  host.call('enhance', req).then((res) => {\r\n    if (seq !== s.seq) return;\r\n    const r = res && typeof res === 'object' ? res : {};\r\n    if (r.ok && Array.isArray(r.clarify) && r.clarify.length > 0) {\r\n      // v4.0.0（专家档澄清卡）：澄清信号分支——不写草稿、不写记忆、不置 optimized；\r\n      // 题目入 store 置 phase='clarify'（ClarifyPanel 渲染输入框下方选项卡），backup 保留供取消恢复\r\n      s.phase = 'clarify';\r\n      s.clarify = r.clarify;\r\n      s.enhanced = '';\r\n      s.error = null;\r\n      notify(sessionId);\r\n      return;\r\n    }\r\n    if (r.ok && typeof r.text === 'string' && r.text !== '') {\r\n      const away = getActiveSession() !== sessionId;\r\n      if (!away && draftRef.current !== s.backup) {\r\n        // 结果被丢弃（增强中用户编辑草稿）：不替换、不写记忆、不打标记（L1）\r\n        s.phase = 'idle';\r\n        s.enhanced = '';\r\n        s.error = null;\r\n        s.clarifyAnswers = []; // v4.0.0（澄清卡）：本轮废弃，暂存问答一并清除\r\n        clearResultStore(sessionId); // v3.3.x-fix：丢弃即失效持久化\r\n      } else {\r\n        const finalText = parts.prefix + r.text;\r\n        if (!away) safeSetDraft(inputActions, finalText); // v3.3.x：切走时不注入（防串会话），结果暂存待回归应用\r\n        saveResultStore(sessionId, s.backup, finalText); // v3.3.x-fix：持久化此前从未接线\r\n        s.phase = 'result';\r\n        s.enhanced = finalText;\r\n        s.error = null;\r\n        // v2.6.2（继续优化标记）：成功应用过结果 → 后续修改草稿时按钮显示「继续优化」\r\n        s.optimized = true;\r\n        // v3.1.3（按钮反馈）→ v4.0.0：记录实际执行模型与是否 fallback（非首个模型完成）——\r\n        // 结果按钮文案已收敛为「撤销优化」（resultFallback 的 {model} 分支已删），此信息保留在\r\n        // store 供诊断/未来展示，不再直接上屏\r\n        s.lastModel = r.model || '';\r\n        s.fallbackUsed = r.fallbackUsed === true;\r\n        // v3.3.x：结果持久化（跨刷新可恢复）\r\n        // v2.2（§6.5/R1）：仅结果已应用且记忆开关开启时写入记忆（斜杠命令存正文）并打标；\r\n        // v2.6.1（记忆链）：追加本轮 {input, output} 并截断到最近 MEMORY_ROUNDS_MAX 轮\r\n        // v4.0.0（澄清卡）：澄清轮的问答先于终稿入链（{input:q, output:a} 与普通轮同构，\r\n        // 继续优化轮次含澄清条目 → host CONTINUE_PROMPT 不重复追问）\r\n        if (config.memory) {\r\n          for (const qa of s.clarifyAnswers) s.memoryRounds.push({ input: qa.q, output: qa.a });\r\n          s.memoryRounds.push({ input: parts.body, output: r.text });\r\n          while (s.memoryRounds.length > MEMORY_ROUNDS_MAX) s.memoryRounds.shift();\r\n          writeSeen(sessionId);\r\n        }\r\n        s.clarifyAnswers = [];\r\n      }\r\n    } else {\r\n      safeSetDraft(inputActions, s.backup);\r\n      s.phase = 'idle';\r\n      s.enhanced = '';\r\n      s.error = r.code && errorKey(r.code) !== 'errUNKNOWN' ? r.code : 'UNKNOWN';\r\n      s.clarifyAnswers = []; // v4.0.0（澄清卡）：失败即弃暂存问答\r\n      clearResultStore(sessionId);\r\n    }\r\n    notify(sessionId);\r\n  }).catch(() => {\r\n    if (seq !== s.seq) return;\r\n    safeSetDraft(inputActions, s.backup);\r\n    s.phase = 'idle';\r\n    s.enhanced = '';\r\n    s.error = 'NETWORK';\r\n    s.clarifyAnswers = []; // v4.0.0（澄清卡）：失败即弃暂存问答\r\n    clearResultStore(sessionId);\r\n    notify(sessionId);\r\n  });\r\n}\r\n\r\nfunction undo(sessionId, inputActions) {\r\n  const s = storeFor(sessionId);\r\n  if (s.phase !== 'result') return;\r\n  safeSetDraft(inputActions, s.backup);\r\n  s.phase = 'idle';\r\n  s.enhanced = '';\r\n  s.error = null;\r\n  // v2.1（§2.4）：撤回 = 放弃上轮结果 = 清除其记忆（语义一致）；\r\n  // v2.6.1（记忆链）：只弹出最后一轮——更早轮次仍属本轮迭代历史，继续有效\r\n  s.memoryRounds.pop();\r\n  // v2.6.2（继续优化标记）：撤回 = 回到起点，重新开始算「首次优化」\r\n  s.optimized = false;\r\n  clearResultStore(sessionId); // v3.3.x\r\n  notify(sessionId);\r\n}\r\n\r\nfunction guardPasses(draft, input) {\r\n  if (typeof draft !== 'string') return false;\r\n  const t = draft.trim();\r\n  if (t === '') return false;\r\n  // v16（§3.6）：/命令 + 正文 → 允许（enhance 时保留前缀只优化正文）；纯 /命令 → 禁用\r\n  if (t.startsWith('/') && !/^\\/\\S+\\s+\\S+/.test(t)) return false;\r\n  if (!input) return false;\r\n  // 提交/裁定窗口为真实锁定期；claimed（@引用/命令占位）放行\r\n  if (input.phase === 'adjudicating' || input.phase === 'submitting') return false;\r\n  return true;\r\n}\r\n\r\n";
